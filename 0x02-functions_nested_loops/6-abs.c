#include "main.h"
/**
 * _abs(int) - function that print abs of an int
 * @n: absolute of n is printed
 * Return:  return abs of n
 */
int _abs(int n)
{
if (n > 0)
return (n);
else
{
n = -1 * n;
return (n);
}

}
