#include "main.h"
#include <stdio.h>
/**
 * mul - function to check letter case
 * @a: first operand
 * @b: second operand
 * Return: return the result of mul of the 2 nos
 */

int mul(int a, int b)
{
return (a * b);

}